# Contributing

## Development workflow

- Branch from `main` using `feature/<name>` or `fix/<name>`
- Use **Conventional Commits** (see `RELEASE.md`)
- Keep PRs small, focused, and tested

## Setup

```bash
make install
pre-commit install
```
