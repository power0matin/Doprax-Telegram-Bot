# name: CI

# on:
#   push:
#     branches: ["main"]
#   pull_request:

# concurrency:
#   group: ci-${{ github.ref }}
#   cancel-in-progress: true

# jobs:
#   lint:
#     name: Lint (Ruff)
#     runs-on: ubuntu-latest

#     steps:
#       - uses: actions/checkout@v4

#       - uses: actions/setup-python@v5
#         with:
#           python-version: "3.11"
#           cache: "pip"
#           cache-dependency-path: |
#             pyproject.toml
#             setup.cfg
#             requirements*.txt

#       - name: Install dependencies
#         run: |
#           python -m pip install -U pip
#           pip install -e ".[dev]"

#       - name: Ruff lint
#         run: ruff check . --output-format=github

#       - name: Ruff format check
#         run: ruff format --check .

#   typecheck:
#     name: Type Check (mypy)
#     runs-on: ubuntu-latest
#     needs: lint

#     steps:
#       - uses: actions/checkout@v4

#       - uses: actions/setup-python@v5
#         with:
#           python-version: "3.11"
#           cache: "pip"
#           cache-dependency-path: |
#             pyproject.toml
#             setup.cfg
#             requirements*.txt

#       - name: Install dependencies
#         run: |
#           python -m pip install -U pip
#           pip install -e ".[dev]"

#       - name: Run mypy
#         run: mypy src tests --show-error-codes --pretty

#   tests:
#     name: Tests (pytest)
#     runs-on: ubuntu-latest
#     needs: typecheck

#     steps:
#       - uses: actions/checkout@v4

#       - uses: actions/setup-python@v5
#         with:
#           python-version: "3.11"
#           cache: "pip"
#           cache-dependency-path: |
#             pyproject.toml
#             setup.cfg
#             requirements*.txt

#       - name: Install dependencies
#         run: |
#           python -m pip install -U pip
#           pip install -e ".[dev]"

#       - name: Run tests
#         run: pytest -q -vv --maxfail=1 --disable-warnings

#   build:
#     name: Build package
#     runs-on: ubuntu-latest
#     needs: tests

#     steps:
#       - uses: actions/checkout@v4

#       - uses: actions/setup-python@v5
#         with:
#           python-version: "3.11"

#       - name: Install build tools
#         run: |
#           python -m pip install -U pip
#           pip install build

#       - name: Build distribution
#         run: python -m build

#       - name: Upload artifacts
#         uses: actions/upload-artifact@v4
#         with:
#           name: dist
#           path: dist/
